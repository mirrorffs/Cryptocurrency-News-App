import React from 'react';
import VerticalList from './lists/VerticalList';

const RegulationNews = ({ data }) => {
  return <VerticalList title='Regulation News' data={data} />;
};

export default RegulationNews;
